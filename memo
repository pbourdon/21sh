Tests unitaires a faire dans history.c pour tout re-tester apres implemantation dans son code

Empecher l'enregistrement dans l'historique de trucs genre !! // !-1 etc =>>>  boucle infinie
(notamment via les fonctions de l'historique qui permettent de rajouter des trucs dans le fichier historique ou dans la liste historique)

Gerer les doubles commandes comprenant !!  ou tout autre commande du genre
genre : echo "alpha" ; !!

A VERIFIER JUSTE AVANT PASSAGE DANS LE MIXER
A chaque passage dans le mixer (ft_choose), check qu'il ne comprends pas de !. 
Si la ligne en comprends un, le remplacer avec un ft_get_elemet_from_list 

SAUF SI C'EST !#, auquel cas faut lui envoyer la ligne entiere via ft_exclamation_diez.c

revoir toutes les options de tous les builtins qu'on croit avoir fais, genre env -u / env -i

voir la valeur de retour sur exit et sur tous les autres builtins

Voir comment stocker les variables sous export et les fonctions

Rajouter dans l'historique une fois que la fonction est faite
